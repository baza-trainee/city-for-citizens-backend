// імпортуємо dotenv для взяття наших секретних даних з .env
require("dotenv").config();

// імпортуємо app і майбутню базу даних

// const app = require("./src/app");

// змінні з .env
// const PORT = process.env.PORT || 3030;
// const HOST = process.env.HOST || "localhost";

// тут буде підключення до бази даних на зразок цього
/*
mongoose
  .connect(DB_HOST)
  .then(() => app.listen(3000))
  .catch((error) => console.log(error.message));

*/
